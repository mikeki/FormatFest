<% form_tag busqueda_path, :method => "get" do%>
<p>
<%= text_field_with_auto_complete :user, :email, {:value => ""}, {:url => formatted_users_path(:js), :method => :get, :with => "'search=' + element.value"} %>

<%= submit_tag "Buscar", :name => nil%>
</p>
<% end %>
